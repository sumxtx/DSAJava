```java
public class ArraysUtil
{  
    public void printArray(int arr[])  
    {
            // Print Elements of an integer array
    }
    public static int[] removeEven(int arr[])  
    {
            // Remove the even numbers off an array
    }
    public static int[] removeOdds(int arr[])  
    {
            // Remove the odds numbers off an array
    }
    public void reverseArray(int arr[], int start, int end)  
    {
            // Reverse the order of the elements on an integer array
    }
    public int findMininum(int arr[])  
    {
            // Find the minimum value of an integer array
    }
    public int findMaximum(int arr[])  
    {
            // Find the minimum value of an integer array
    }
    public int secondMaximum(int arr[])  
    {
            // Find the maximum value of an integer array
    }
    public void moveElements(int arr[], int l, int n)  
    {
            // Move all the "n" elements of an array to its end
            // Preserving the order of the others elements
    }
    public void expandArray_buggy(int arr[], int capacity)  
    {
            // Increase an array size, only on its scope
    }
    public void expandArray(int arr[], int capacity)  
    {
            // Increase an array size
    }
    public int findMisingNumber(int arr[])
    {
            // Find the missing element of an unsorted array
            // Assuming they where sequential
            // Ex: 1,5,2,3 -> 4
    }
    public boolean isPalindrome(String word)
    {
            // Checks if a string is palindrome or not
    }

    public static void main(String args[]) {//}
```
